<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.security.dao.LoginDao" >

    <select id="login" parameterType="String" resultType="com.demo.common.vo.UserVo">
    /* Login.xml - login */
        SELECT
            *
        FROM MEMBER
        WHERE id = #{id}
    </select>

</mapper>